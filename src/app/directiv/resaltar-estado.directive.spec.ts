import { ResaltarEstadoDirective } from './resaltar-estado.directive';

describe('ResaltarEstadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarEstadoDirective();
    expect(directive).toBeTruthy();
  });
});
